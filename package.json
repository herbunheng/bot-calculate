{
	"name": "bot-calculate",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"d1:local:migrate": "wrangler d1 execute bot-db --local --file schema.sql",
		"d1:remote:migrate": "wrangler d1 execute bot-db --remote --file schema.sql",
		"dev": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"@types/node": "^25.6.0",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.84.1"
	}
}
